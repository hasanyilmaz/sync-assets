import { Plugin } from "obsidian";

/**
 * Sync Assets project scaffold.
 *
 * Functional behavior will be implemented only after the project plan is
 * approved in its own workspace.
 */
export default class SyncAssetsPlugin extends Plugin {}
